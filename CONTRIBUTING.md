# CONTRIBUTING

## 기본 원칙
- 문제 단위로 작게 작업하고 커밋도 문제/단계 단위로 분리한다.
- 알고리즘 함수는 가능한 순수 함수 형태를 유지한다.
- Step 생성기는 UI 렌더 가능한 직렬화 가능한 payload만 사용한다.

## 권장 워크플로우
1. 문제 선택 (`Pxxx`)
2. TODO 구현 (`src/algorithms`)
3. 테스트 작성/보강 (`test`)
4. 시각화 Step 생성기 구현 (`src/visualizers`)
5. 레지스트리 등록 (`src/registry`, `docs/problems.js`)
6. UI 확인 (`npm run dev`)
7. 커밋
8. Codex 리뷰 요청

## 커밋 메시지 규칙
- 한국어 + 단계 prefix 권장
- 예시
  - `D1-1 P001 Two Sum 문제/구현/스텝/테스트 추가`
  - `D1-2 P001 레지스트리/문서 UI 연결 및 렌더러 확장`

## Codex 리뷰 요청 체크리스트
- [ ] 시간복잡도/공간복잡도 적절한가?
- [ ] 엣지케이스(빈입력, 경계값, 중복 등) 테스트가 있는가?
- [ ] 불필요한 부작용/변이(mutability)가 없는가?
- [ ] Step 흐름이 `init -> ... -> done`으로 일관적인가?
- [ ] UI에서 상태(payload)가 이해 가능하게 표시되는가?
- [ ] 네이밍/가독성/중복 코드 개선 여지가 있는가?

## PR 본문 권장 템플릿
- 변경 배경
- 변경 파일/기능 요약
- 테스트 결과 (성공/환경 제한)
- 다음 단계
